# .NET
bin/
obj/
*.user
[Bb]uild/
[Dd]ebug/
[Rr]elease/
TestResults/
*.trx

# Flutter / Dart (for when the app lands)
.dart_tool/
.packages
build/
.flutter-plugins
.flutter-plugins-dependencies
*.iml

# Editors
.vs/
.vscode/
.idea/
*.swp
.DS_Store

# Local secrets — never commit
appsettings.Local.json
.env

# XInfo legacy DB backups (real customer data, tens of GB) — local dev restore only, never commit
/XInfoDB/

# Flutter app
app/.dart_tool/
app/build/
app/.flutter-plugins
app/.flutter-plugins-dependencies
app/ios/Pods/
app/ios/.symlinks/
app/android/.gradle/
app/android/local.properties
